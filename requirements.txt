# ──────────────────────────────────────────────────────────────
# Install PyTorch with XPU (Intel GPU) support FIRST:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
# Then install the rest:
#   pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# Core ML / Deep Learning  (torch installed separately via XPU index)
timm>=0.9.0

# Pose estimation inference (ONNX, no mmcv/mmdet needed)
rtmlib>=0.0.15
onnxruntime>=1.17.0

# Intel acceleration (optional – for OpenVINO EP in onnxruntime)
openvino>=2024.0.0

# Computer Vision & Media
opencv-python>=4.8.0
Pillow>=10.0.0

# Numerical / Scientific
numpy>=1.24.0
scipy>=1.11.0

# Visualization
matplotlib>=3.7.0

# Utilities
tqdm>=4.65.0
